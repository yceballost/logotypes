{
  "builds": [
    { "src": "app.py", "use": "@vercel/python" },
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1" },
    { "src": "/favicon.ico", "dest": "/favicon.ico" }
    { "src": "/(.*)", "dest": "app.py" }
  ]
}
